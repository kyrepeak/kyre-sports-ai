name: WNBA Step19E guarded production release (retired)

on:
  push:
    branches: [wnba-step19e-production-release-20260829]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: wnba-step19e-production-release
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded by Step19F production certification
        run: |
          echo "STEP19E_RELEASE_RETIRED"
          echo "Production deployment is owned by wnba-step19e-consumer-fix-release.yml targeting the exact certified Step19F SHA."
