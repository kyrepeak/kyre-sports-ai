name: NFL Passing Yards Why Step 2 QB Cards

on:
  push:
    branches:
      - "monster/nfl-passing-why-step2-qb-cards-v1"
    paths:
      - "nfl_passing_yards_hub_v36.py"
      - "tests/test_nfl_passing_yards_why_step2_qb_cards_v1.py"
      - ".github/workflows/nfl-passing-why-step2-qb-cards-v1.yml"

permissions:
  contents: read

jobs:
  targeted-proof:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install targeted test stack
        run: |
          python -m pip install --disable-pip-version-check -q -r requirements.txt
          python -m pip install --disable-pip-version-check -q pytest
      - name: Prove Step 2 premium QB identity cards
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: |
          python -m pytest -q tests/test_nfl_passing_yards_why_step2_qb_cards_v1.py
          echo "NFL_PASSING_WHY_STEP2_QB_CARDS_GREEN"
