name: Monster A8 Runtime Detection TDD

on:
  pull_request:
    branches:
      - main

jobs:
  a8-runtime-detection:
    if: github.head_ref == 'monster-a8-streamlit-runtime-detection-fix'
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install focused test dependencies
        run: python -m pip install --disable-pip-version-check pytest streamlit
      - name: Run complete A8 telemetry and Error Radar contracts
        run: python -m pytest -q tests/test_streamlit_telemetry_activation_probe_v1.py tests/test_monster_error_radar_v1.py
