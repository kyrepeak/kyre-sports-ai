name: NFL Game Totals Page Step 5

on:
  push:
    branches:
      - monster/nfl-game-totals-page-step5
  pull_request:
    branches:
      - main
    paths:
      - nfl_game_totals_hub_v5.py
      - nfl_hub_v18.py
      - sports_api/nfl_game_totals_explosive_context_v1.py
      - tests/test_nfl_game_totals_page_step5.py
      - .github/workflows/nfl-game-totals-page-step5.yml
  workflow_dispatch:

jobs:
  page-step5-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install narrow test dependencies
        run: python -m pip install --upgrade pip pytest requests
      - name: Freeze-scope guard
        run: |
          python - <<'PY'
          import subprocess
          allowed = {
              'nfl_game_totals_hub_v5.py',
              'nfl_hub_v18.py',
              'sports_api/nfl_game_totals_explosive_context_v1.py',
              'tests/test_nfl_game_totals_page_step5.py',
              '.github/workflows/nfl-game-totals-page-step5.yml',
          }
          changed = set(subprocess.check_output(['git','diff','--name-only','origin/main...HEAD'], text=True).splitlines())
          extra = sorted(changed - allowed)
          if extra:
              raise SystemExit(f'Unexpected Step 5 paths: {extra}')
          forbidden = [p for p in changed if any(token in p.lower() for token in ('spread', 'moneyline', 'rushing_yards', 'receiving_yards', 'v136'))]
          if forbidden:
              raise SystemExit(f'Frozen surface changed: {forbidden}')
          print('freeze-scope OK:', sorted(changed))
          PY
      - name: Run Step 5 contract
        run: python -m pytest -q tests/test_nfl_game_totals_page_step5.py
      - name: Compile Step 5 runtime files
        run: python -m py_compile nfl_game_totals_hub_v5.py sports_api/nfl_game_totals_explosive_context_v1.py nfl_hub_v18.py
