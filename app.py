"""Kyre Sports AI Streamlit entrypoint — CFB O/U readable Step 11 form live.

Router V68 keeps Router V67 / Clean Page V27 certified and activates additive
Clean Page V28 for College Football Over/Under only.

Clean Page V28 preserves readable Steps 4-10, certified Market Adapter V2
freshness + exact official ESPN event-ID attachment, and Step 5C market
intelligence at 0.0% projection influence. It changes only Step 11
presentation, translating the already-certified current-form and
schedule-strength engine output into readable current-season evidence.
Current-form context is bounded by the certified engine's existing contract;
the presentation layer cannot move the frozen projected total, analysis line,
selection, or certification. No fuzzy matching. No synthetic IDs. Frozen
Schedule V5 and Steps 3-12 projection math remain unchanged.

Deployment heartbeat:
STREAMLIT_MAIN_V68_CFB_OU_READABLE_STEP11_2026-09-10T23:30Z.
"""
from __future__ import annotations

# Retain explicit frozen predecessor imports so the permanent DevSystem guard
# continues proving older active contracts while V68 is the additive top layer.
from streamlit_memory_lazy_router_v63 import render_app as _frozen_v63_render_app
from streamlit_memory_lazy_router_v64 import render_app as _frozen_v64_render_app
from streamlit_memory_lazy_router_v65 import render_app as _frozen_v65_render_app
from streamlit_memory_lazy_router_v66 import render_app as _frozen_v66_render_app
from streamlit_memory_lazy_router_v67 import render_app as _frozen_v67_render_app
from streamlit_memory_lazy_router_v68 import render_app

DEPLOYMENT_HEARTBEAT = "STREAMLIT_MAIN_V68_CFB_OU_READABLE_STEP11_2026-09-10T23:30Z"

render_app()
